<div id="contentsu" class="top-padi">
 <div id="contentsu-in" class="clearfix" >

  <%= form_for @inquiry, :url => inquiry_confirm_path do |f| %>

    <% if @inquiry.errors.any? %>
    <div class="top-margin">
      <div class="alert alert-danger" role="alert">
        <strong>入力内容にエラーがあります</strong>
        <ul>
          <% @inquiry.errors.each do |attr, msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    </div>
    <% end %>
    
  <div class="top-margin">
    <%= render 'shared/sidebar' %>

    <div id="main-footer">
        <h3>
         <%= image_tag("footer/otoiawase_midashi.png", size: "710x90", alt: "お問い合わせ") %>
        </h3>
      <div class="sections">
        <h4>
         <%= image_tag("footer/contact-1.jpg", size: "710x37", alt: "STEP1") %>
        </h4>
         <p>
         9-NINEのご利用についてご不明な点がございましたら、以下のフォームに必要事項をご入力の上、「入力内容を確認する」ボタンを押してください。
         </p>

      <table class="tables">
      <tr>
        <th>お名前<span class="text-danger">（必須）</span></th>
        <td><%= f.text_field :name %><span> (全角) </span></td>
      </tr>
      <tr>
        <th>メールアドレス<span class="text-danger">（必須）</span></th>
        <td><%= f.text_field :email, :size => "40x1" %><span> (半角英数字) </span></td>
      </tr>
      <tr>
        <th>電話番号</th>
        <td><%= f.text_field :tel %><span> (半角数字) </span></td>
      </tr>
      <tr>
        <th>お問い合わせ内容<span class="text-danger">（必須）</span></th>
        <td><%= f.text_area :message, :maxlength => "4000", :size => "60x6" %><span> (4000文字以内) </span></td>
      </tr>
      </table>
        
      <p><%= f.submit '入力内要を確認する', class: 'btn btn-primary btn-1' %></p>
      </div><!--sections end-->
    </div><!--main-footer end-->
  </div>
  <% end %>
    
  
 </div><!-- contents-in -->
</div><!--contentsu end-->